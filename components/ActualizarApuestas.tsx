import { View, Text } from 'react-native'
import React from 'react'
import { Retirado } from '@/lib/types';

const ActualizarApuestas = ({
  retirados,
}: {
  retirados: Retirado[];
})  => {
  return (
    <View>
      <Text>ActualizarApuestas</Text>
    </View>
  )
}

export default ActualizarApuestas